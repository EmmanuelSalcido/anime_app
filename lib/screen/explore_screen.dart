import 'package:flutter/material.dart';

class ExploreScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Coloca el contenido de la pantalla "Explorar" aquí
    return Center(
      child: Text('Pantalla Explorar'),
    );
  }
}
