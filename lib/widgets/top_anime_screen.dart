import 'package:flutter/material.dart';

class TopAnimeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Top Animes'),
      ),
      body: Center(
        child: Text('Contenido de Top Anime Screen'),
      ),
    );
  }
}
